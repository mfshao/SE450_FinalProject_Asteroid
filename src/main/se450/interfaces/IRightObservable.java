package main.se450.interfaces;

/**
 * The Interface IRightObservable represents a right event observer.
 */
public interface IRightObservable extends IObservable {
	
	/**
	 * Execute a right event.
	 */
	void right();
}
